{% extends 'base.html.twig' %}

{% block body %}
    <h2>{{ article.title }}</h2>
    <p>Views: {{ article.countView }}</p>
    <ul>
        {% for tag in article.tags %}
            <li>{{ tag.name }}</li>
        {% endfor %}
    </ul>
    <p>{{ article.content }}</p>
{% endblock %}